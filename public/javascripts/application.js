// const express = require('express');
// const mongoose = require('mongoose');

// const usersRouter = require('./routes/users');

// const router = express.Router();

// let buttonSignup = document.getElementsByClassName('button_signup');

// buttonSignup.addEventListener('click', async (event) => {
//   if (event.target.dataset.item) {
//     const ftch = await fetch(`/items/${event.target.dataset.item}`, { method: 'GET' })
//   }
// })
